# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets / environment (Vault AppRole creds, API keys, ...)
.env

# Test & coverage reports (CI uploads these as artifacts)
.coverage
coverage.xml
junit.xml
htmlcov/

# Pyright report consumed by the ratchet (scripts/pyright_ratchet.py)
pyright.json

# prompt_builder on-disk extraction cache
.prompt_builder_cache/

# Reference PDFs — user-provided, copyrighted SAS/O'Reilly manuals; never commit
reference_docs/
