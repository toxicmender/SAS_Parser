"""
memory — LangChain chat-history and KV persistence layer.

A regular package (not a PEP-420 namespace package) so packaging tools
and import machinery treat it uniformly.  The implementation lives in
:mod:`memory.short_mem`; import from there:

    from memory.short_mem import DatabricksMemory
"""
