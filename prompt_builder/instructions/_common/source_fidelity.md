## [kind: DATA_STEP, PROC_STEP, MACRO_DEFINITION, MACRO_CALL, MACRO_CONTROL_FLOW] Documented SAS semantics

Identify the exact SAS language element, procedure, or action before mapping it.
Use the retrieved local reference section as the source of truth for its inputs,
execution timing, ordering, missing-value behaviour, and outputs; a similarly
named target operation is not evidence that the semantics match. Where the
available guidance does not establish an equivalent, preserve the source intent
as explicitly as the target allows and record the unsupported or assumed
behaviour under Risks instead of guessing.
