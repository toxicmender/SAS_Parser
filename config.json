{
  "_comment": "Runtime limits for SAS_Parser. null means 'unset' (the code default applies). Precedence: explicit constructor argument > this file > code default. Loaded via the app_config package; point SAS_PARSER_CONFIG at an alternative file to override.",
  "sas_chunker": {
    "min_words": 300,
    "max_words": 700
  },
  "instruction_chunker": {
    "min_words": 120,
    "max_words": 900,
    "overlap_words": 60
  },
  "prompt_builder": {
    "top_k": 6,
    "max_instruction_words": 1500,
    "focus_hints": null,
    "reasoning_directives": null
  },
  "llm_client": {
    "model": null,
    "base_url": null,
    "url_headers": null,
    "timeout": null,
    "cert_file": null,
    "temperature": null,
    "max_retries": null,
    "model_kwargs": null,
    "max_input_tokens": null,
    "max_output_tokens": null,
    "prompt_caching": null,
    "requests_per_second": null,
    "max_bucket_size": null
  },
  "_pipeline_comment": "Pipeline behaviour. 'structured_output' asks the model for a typed TranslationDocument (analysis / mapping / ordered notebook cells / risks) instead of free-form Markdown, so pipeline.notebook knows which cells are runnable code rather than guessing from fences. The turn persisted to memory and the item's 'response' stay Markdown either way, so conversation memory, resume, and validation are unaffected; a gateway that will not honour the schema degrades to the model's prose with a warning. Set false to prompt for plain Markdown. 'max_merged_tokens' turns on token-budgeted call packing: adjacent work items (independent singletons AND small dependency batches) share one LLM call while the estimated prompt cost stays under this many tokens, cutting per-call overhead; null keeps packing off (count-capped singleton merging only). Item ids change under packing (packed-NNN), so resume only matches runs made under the same budget.",
  "pipeline": {
    "structured_output": null,
    "max_merged_tokens": null
  },
  "_complexity_comment": "Complexity analysis. 'target' names a rule-set profile under complexity/profiles/ ('sparksql', 'pyspark', ...); 'rules_path' points at a profile file of your own and wins over 'target'. The per-tier weights only rank units within a tier; they can never change a tier, which is presence-based (any HIGH construct makes the unit HIGH). Retune which construct maps to which tier by editing a profile JSON, not here.",
  "_complexity_sizing_comment": "T-shirt sizing (S/M/L/XL per source file). Each of the three dimensions (effort, complexity, uncertainty) is log-transformed and min-max rescaled onto 0-1 against a window declared in the profile as a MULTIPLE of the anchor, then blended by the profile's dimension weights, then rescaled in log space onto the 2-8 point Fibonacci scale. 'size_anchor' is the raw score of the reference MEDIUM file that every window is stated against, so LOWERING it makes every file rate LARGER. 'min_story_points'/'max_story_points' re-denominate the reported points onto your own scale (e.g. 1 and 13 instead of the Fibonacci 2 and 8); they change the NUMBERS only, never a size, because the bands are read as fractions of the span. The rest of the calibration -- band boundaries, min-max bounds, per-dimension weights -- lives in the profile's 'sizes' block; only these knobs are exposed here because they move every verdict coherently. 'use_cross_file' resolves each file's macro/dataset/macro-variable/libref references against the rest of the corpus; turning it off scores every file as if it were the only one.",
  "complexity": {
    "target": null,
    "rules_path": null,
    "weight_low": null,
    "weight_medium": null,
    "weight_high": null,
    "size_anchor": null,
    "min_story_points": null,
    "max_story_points": null,
    "use_cross_file": null
  },
  "user_instructions": {
    "path": null,
    "dir": null,
    "max_words": null
  },
  "_validation_comment": "Per-metric pass thresholds; null uses the metric class default. Every metric is higher-is-better, hallucination_threshold included: that metric reports groundedness (1 - deepeval's hallucination rate), so 0.8 means 'at most 20% of ground-truth contexts contradicted'. The thresholds below llm_judge_threshold belong to the opt-in LLM-judged suite (validation.judged_metrics); they are inert unless it is enabled. prompt_instructions is a list of strings prompt_alignment checks the response against, overriding validation.agentic_metrics.DEFAULT_PROMPT_INSTRUCTIONS.",
  "validation": {
    "response_coverage_threshold": null,
    "dataset_fidelity_threshold": null,
    "python_syntax_threshold": null,
    "required_terms_threshold": null,
    "reference_similarity_threshold": null,
    "llm_judge_threshold": null,
    "prompt_alignment_threshold": null,
    "answer_relevancy_threshold": null,
    "faithfulness_threshold": null,
    "hallucination_threshold": null,
    "contextual_precision_threshold": null,
    "contextual_relevancy_threshold": null,
    "plan_adherence_threshold": null,
    "analysis_summarization_threshold": null,
    "summarization_threshold": null,
    "task_completion_threshold": null,
    "prompt_instructions": null,
    "table": null,
    "path": null
  },
  "_vault_comment": "Non-secret Vault connection settings only. Secrets (VAULT_TOKEN, VAULT_ROLE_ID, VAULT_SECRET_ID) come from the environment, never this file. address/namespace/auth_path/oidc_role/azure_scopes also read from VAULT_ADDR/VAULT_NAMESPACE/VAULT_AUTH_PATH/VAULT_OIDC_ROLE/VAULT_AZURE_SCOPES, which win over these. Setting oidc_role enables 'azuread' login: an Entra ID JWT (see 'azure' and databricks.secret_scope) is presented to the jwt auth method at auth/<auth_path>/login. ai_gateway_key names the field holding the token in the appsvc/ai_gateway secret; null tries token/api_key/apikey/ai_gateway_token/value in order.",
  "vault": {
    "address": null,
    "namespace": null,
    "mount_point": null,
    "kv_version": null,
    "timeout": null,
    "verify": null,
    "auth_path": null,
    "oidc_role": null,
    "azure_scopes": null,
    "ai_gateway_key": null
  },
  "_databricks_comment": "Non-secret Databricks workspace settings only. The token (DATABRICKS_TOKEN) and the service-principal secret (ARM_CLIENT_SECRET) come from the environment, never this file; on a cluster no credential is needed, and off-cluster an Entra ID service principal is preferred over a PAT. Every key here is also read from its standard env var, which wins over this file: DATABRICKS_* for the workspace ones, ARM_TENANT_ID/ARM_CLIENT_ID for the service principal (the names Databricks documents), DATABRICKS_AZURE_RESOURCE_ID for azure_workspace_resource_id, DATABRICKS_SECRET_SCOPE for secret_scope. Set azure_workspace_resource_id only when the service principal is NOT assigned to the workspace. secret_scope names a Databricks secret scope holding the service principal under sp-hsv-tenantid/sp-hsv-appid/sp-hsv-secret; reading it needs a credential of its own (a cluster runtime or a PAT).",
  "databricks": {
    "host": null,
    "http_path": null,
    "warehouse_id": null,
    "cluster_id": null,
    "catalog": null,
    "schema": null,
    "timeout": null,
    "azure_tenant_id": null,
    "azure_client_id": null,
    "azure_workspace_resource_id": null,
    "secret_scope": null
  },
  "_azure_comment": "Non-secret Microsoft Entra ID (Azure AD) settings only. The client secret (AZURE_CLIENT_SECRET) comes from the environment, never this file. tenant_id/client_id/authority_host/scopes/certificate_* also read from AZURE_TENANT_ID/AZURE_CLIENT_ID/AZURE_AUTHORITY_HOST/AZURE_SCOPES/AZURE_CLIENT_CERTIFICATE_PATH/AZURE_CLIENT_CERTIFICATE_THUMBPRINT, which win over these. flow: 'client_credentials' (service principal) or 'device_code' (interactive user login); null infers it from the credentials present.",
  "azure": {
    "tenant_id": null,
    "client_id": null,
    "authority_host": null,
    "scopes": null,
    "flow": null,
    "timeout": null,
    "certificate_path": null,
    "certificate_thumbprint": null
  },
  "_sharepoint_comment": "Non-secret SharePoint (Microsoft Graph) settings only. There is no secret here: authentication is delegated to the 'azure' Entra ID service principal, which mints a Graph token. Identify the site with site_id, or site_hostname + site_path; drive_id targets a specific document library (default: the site's). Every key is also read from its SHAREPOINT_* env var, which wins over this file.",
  "sharepoint": {
    "site_hostname": null,
    "site_path": null,
    "site_id": null,
    "drive_id": null,
    "scopes": null,
    "timeout": null
  },
  "_powerapps_comment": "Which SharePoint list the Power Apps conversion requests live in, and the *internal* names of its columns (which often differ from the display names). Used by 'demo_run.py sharepoint'. list_name is required at run time. Column names default to RequestId/ApplicationName/LiveValidation/Model/Timestamp when null. Every key is also read from its POWERAPPS_* env var (POWERAPPS_LIST_NAME, POWERAPPS_<NAME>_FIELD), which wins over this file.",
  "powerapps": {
    "list_name": null,
    "request_id_field": null,
    "application_name_field": null,
    "live_validation_field": null,
    "model_field": null,
    "timestamp_field": null
  }
}
